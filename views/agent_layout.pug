doctype html
head
  meta(charset='UTF-8')
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  title Mayondo Wood Inventory System
  link(rel='stylesheet' href='css/agent.css')
if flashMessage
    .flash-message(class=flashMessage.type)
        | #{flashMessage.text}
    nav.navbar
      .logo
        | Mayondo 
        span Wood
        if currentUser && (currentUser.role === 'Sales-Agent')
        ul.nav-links
            li
                a.active(href='/') Home
            li
                a(href='/registeredWood') View Wood Stock
            li
                a(href='/registeredFurniture') View Furniture Stock
            li
                a(href='#')  Enter Sale
            li
                a(href='/logout') Logout 
block content 